# This is calculator written in Rust
It can run basic operation like plus, minus, mult and div with 2 variables.
### Building
---
```
git clone https://github.com/SlyQFS/rustulator
cd rustulator
cargo build
```
